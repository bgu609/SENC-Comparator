<UserControl x:Class="SENC_Comparator_NF48_1._0.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SENC_Comparator_NF48_1._0.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <ContentControl>
                <ContentPresenter Content="{Binding Reference_ReadViewer}"/>
            </ContentControl>
        </Grid>

        <Grid Grid.Column="1" Margin="5">
            <ContentControl>
                <ContentPresenter Content="{Binding Check_ReadViewer}"/>
            </ContentControl>
        </Grid>
    </Grid>
</UserControl>